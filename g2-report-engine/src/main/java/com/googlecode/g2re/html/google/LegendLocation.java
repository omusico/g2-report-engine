package com.googlecode.g2re.html.google;

/**
 * Represents if / where the legend is to be rendered on a chart element.
 * @author Brad Rydzewski
 */
public enum LegendLocation {
    top, bottom, left, right, none
}
